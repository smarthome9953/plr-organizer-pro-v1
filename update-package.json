// Script to update package.json with Electron configuration
// Add these properties manually:

{
  "main": "dist-electron/main/index.js",
  "scripts": {
    "electron:dev": "concurrently \"vite --port 8082\" \"wait-on http://localhost:8082 && tsc -p electron/tsconfig.json && electron .\"",
    "electron:build": "vite build && tsc -p electron/tsconfig.json && electron-builder",
    "electron:pack": "vite build && tsc -p electron/tsconfig.json && electron-builder --dir"
  }
}

// Instructions:
// 1. Add "main": "dist-electron/main/index.js" to the root of package.json
// 2. Add the three electron scripts to the "scripts" section
